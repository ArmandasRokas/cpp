#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include "doubly-linked.h"
#include "test.h"

using std::cin;
using std::cout;
using std::string;
using std::vector;
using std::endl;

int main(){
   /* string buf;
    getline(cin, buf);
    if(buf == ""){
        return 0;
    }
    std::istringstream ssin(buf);

    // declare input
    // vector<...> ... ;
    // ... input;
    while(ssin >> input){
        //.push_back(input);
    }
    */

   testDriver();

}

